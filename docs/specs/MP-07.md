# MP-07 — Item request, create

| | |
|---|---|
| **Route** | `/dashboard/items/new` |
| **Access** | Authenticated member with an active membership |
| **Bound** | Yes, matches screenshots |
| **Screenshots** | `docs/screenshots/MP-07-desktop.png`, `docs/screenshots/MP-07-mobile.png` |
| **Depends on** | MP-04 (entry), MP-08 (exit) |
| **Spec status** | Complete |

---

## 1. Purpose

The first step of creating an item request. A member describes the need and who to contact about it. The request is created at `draft` and the member is sent straight to MP-08 to add the individual items. A request without items cannot be submitted, so this surface always hands off rather than completing anything.

## 2. Entry and exit

**Arrives from:** MP-04.
**Leaves to:** MP-08 `/dashboard/items/:id/add`, automatic redirect on save, carrying the new request's id. Back control returns to MP-04.

A member who abandons this form before saving creates nothing.

## 3. Data

**Reads:** `organizations.name` for the session organization, to display.

**Writes, in one transaction:** `item_requests` at `status = 'draft'`, `org_id` from the session, `created_by` set to the acting user. `people`, or resolves an existing row by `lower(email)`, for the request contact, then sets `contact_person_id`.

**Functions called:** None.

**Never touches:** `submitted_at`, `approved_at`, `approved_by`, `archived_at`, `archived_reason`. No status other than `draft`. No `approval_events` row; the request has not transitioned from anything yet, and its first event is written at MP-08 on submit.

## 4. Layout regions

Confirmed order: heading and hero, two paragraphs of instructional copy, organization (read-only), contact fields, deadline type, people helped, request title with helper text, request description with helper text, save button. No image upload control anywhere on this surface, and no dropoff location field, both confirmed absent, matching D11's existing design (staff add the image at ADMIN-02, not the member at creation).

## 5. Fields

| # | Label (verbatim) | Control | Required | Binds to | Validation |
|---|---|---|---|---|---|
| 1 | Contact Full Name | Text | Yes | `people.first_name` / `people.last_name` | Live site uses one combined field; build as two per D41 regardless, same as every other form in the app |
| 2 | Contact Email Address | Email | Yes | `people.email` | Valid email |
| 3 | Contact Phone Number | Text | Yes | `people.phone` | |
| 4 | Deadline Type | Radio group | Yes | `item_requests.deadline_type` | Three options, confirmed: Ongoing, Until Fufilled, Date Specific. Live label is misspelled, verbatim copy is verbatim, keep the typo, don't correct it |
| 5 | How many kids and/or families will this help? | Number | Yes | `item_requests.people_helped` | Integer, zero or greater |
| 6 | Request Title | Text | Yes | `item_requests.title` | Helper text below, verbatim: "In 5-10 words please let people know what is needed and who this will serve." Example line: "Examples: Single Dad in Need of a Stroller \| Bike Needed for Refugee Teen \| Diapers for a Child Receiving Cancer Treatment" |
| 7 | Request Description | Textarea | Yes | `item_requests.description` | Helper text below, verbatim: "In 2-4 sentences please give people more details about the youth or family in need and the impact that receiving these items will have." Example line: "Examples: Single dad who just found stable housing for himself and his baby is in need of a stroller to be able to transport his child. Dad has back problems so providing the stroller would help meet many needs." |
| 8 | Organization | Read-only text | n/a | `organizations.name`, session | |

No `image_url` field, no `dropoff_location` field, confirmed absent from this surface, both closed.

**Deadline date picker.** When field 4 is Date Specific, a date picker appears and is required. Stored as `item_requests.deadline_date`. The stored values for deadline type are `ongoing`, `until_fulfilled`, and `date_specific`.

**`expires_on` does not appear on this surface.** It is set by staff, not by the member. Do not add a field for it.

## 6. Actions

**Continue to Add Items**
- Enabled when: all required fields pass validation.
- Does: the write in section 3.
- Transaction boundary: the request and the contact person are written together or not at all.
- Emails queued: none. Nothing is submitted yet.
- On success: redirect to MP-08 for the new request.
- On failure: nothing written, form retains every value, failure message renders.

**Back control**
- Navigates to MP-04. No write.

## 7. Conditional behavior

| Trigger | Result |
|---|---|
| Deadline type is `date_specific` | Date picker visible and required |
| Deadline type is any other value | Date picker hidden. `deadline_date` left null |

This is the shared deadline control described in `Handbook.md` section 11. Identical on MP-09, MP-10, and MP-12. Build it once.

## 8. Copy

| Context | Text |
|---|---|
| Hero text | Item Request |
| Page heading | POST A DONATION NEED |
| Instructional copy, paragraph 1 | Please complete the form below to create a new item donation request. On this page you will provide the details about the child, teen, parent or caregiver this request will benefit them you will be able to add items to the request on the following page. |
| Instructional copy, paragraph 2 | Make your request compelling by keeping the post title and description brief but including as much personal information as you can to inspire people to meet this need. |
| Save button | Continue to Add Items |
| Failure message | Something went wrong and your request wasn't saved. Please try again. |
| Field-level validation messages | Standard per-field required messaging, nothing site-specific captured |

Paragraph 1 is reproduced exactly as captured, including its run-on construction. Do not correct the grammar; verbatim is verbatim.

The failure message is **written, not captured** — no failure state appeared in the capture. Wording matches this form's own voice.

## 9. Empty states

None. The form always renders complete.

## 10. Mobile differences

Closed per D60. Single column, same fields in the same order, full-width controls.

## 11. Authorization

Verify the session user holds an active membership, server-side. `org_id` on the new request comes from the session, never from field 8 or any client value.

## 12. Error paths

| Failure | Rendered result |
|---|---|
| Contact email matches an existing person | Not an error. Attach the existing row. One human, one row |
| Transaction fails partway | Nothing written. No orphan request with no contact, no orphan person with no request |
| Deadline type is `date_specific` with no date | Blocked at the surface. The database constraint also rejects it, but the member should never reach that |

## 13. Out of scope for this surface

- Adding items. MP-08.
- Submitting for approval. MP-08.
- Setting status to anything other than `draft`.
- Setting `expires_on`.

## 14. Acceptance

- A saved request lands at `status = 'draft'` with `org_id` from the session.
- Saving redirects to MP-08 for that request.
- The date picker appears only for `date_specific` and the date is required when it appears.
- No `approval_events` row and no email are produced by this surface.
- A contact email that already exists creates no duplicate person.
- First and last name land in two separate columns, never concatenated.
- A single-word name in either name field succeeds.
- The organization display field cannot influence which organization the request is created against. Verify by tampering.
- An abandoned form creates nothing.
- Field order, labels, and copy match the desktop screenshot. Mobile matches the mobile screenshot.

## 15. Open captures

None.
