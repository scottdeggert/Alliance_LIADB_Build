# MP-04 — Organization dashboard

| | |
|---|---|
| **Route** | `/dashboard` |
| **Access** | Authenticated member with an active membership |
| **Bound** | Yes, matches screenshots |
| **Screenshots** | `docs/screenshots/MP-04-desktop.png`, `docs/screenshots/MP-04-mobile.png` |
| **Depends on** | MP-02 (resolves which organization this is) |
| **Spec status** | Open captures listed in section 15 |

---

## 1. Purpose

The organization's home in the portal and the hub every other member surface hangs off. It lists the organization's existing item and volunteer requests for editing, and provides the routes to create new ones, manage the organization, invite members, and view supporters.

## 2. Entry and exit

**Arrives from:** MP-02 after authentication, the dashboard control in the navigation, and the back control on every other member surface.

**Leaves to:**

| Destination | Trigger |
|---|---|
| MP-09 `/dashboard/items/:id/edit` | Selecting an item request and activating its edit control |
| MP-12 `/dashboard/volunteer/:id/edit` | Selecting a volunteer request and activating its edit control |
| MP-07 `/dashboard/items/new` | Create item request |
| MP-10 `/dashboard/volunteer/new` | Create volunteer request |
| MP-05 `/dashboard/organization` | Organization settings |
| MP-06 `/dashboard/members/new` | Add a member |
| MP-13 `/dashboard/supporters` | View supporters |

## 3. Data

**Reads:** `item_requests` and `volunteer_requests` for the session's organization (id, title, created_at, status), `organizations` (name).
**Writes:** Nothing.
**Functions called:** None.
**Never touches:** anything. This surface is read-only.

## 4. Layout regions

1. **Heading**, `[CAPTURE]` whether it names the organization.
2. **Item requests region.** Selector plus edit control.
3. **Volunteer requests region.** Selector plus edit control.
4. **Navigation region.** Links to create, manage, and view surfaces.

`[CAPTURE]` the region order and whether the navigation links are grouped, listed, or presented as buttons.

## 5. Fields

| # | Label (verbatim) | Control | Required | Binds to | Validation |
|---|---|---|---|---|---|
| 1 | `[CAPTURE]` | Select | n/a | Item request selection, local only | |
| 2 | `[CAPTURE]` | Select | n/a | Volunteer request selection, local only | |

**Option label format.** Both selectors render each entry as the request title followed by its creation date. `[CAPTURE]` the exact separator and date format. The current system uses a form of "Title - Date Created"; the literal separator characters and the date format both need capturing, because members scan these lists by date and a changed format is immediately noticeable.

**Which requests appear.** `[CAPTURE]` whether the selectors list all statuses or filter to a subset. This is a dataset-level filter on the current system and appears in no readable file. Until confirmed, the default is: all requests belonging to the organization regardless of status, so a member can reach a `draft` request left mid-creation and an `archived` one they want to review.

**Sort order.** `[CAPTURE]`. Until confirmed, most recently created first.

## 6. Actions

**`[CAPTURE]` item request edit button label**
- Enabled when: `[CAPTURE]` whether it is disabled with no selection or navigates only on a valid selection.
- Does: navigates to MP-09 for the selected request.
- Emails queued: none.

**`[CAPTURE]` volunteer request edit button label**
- Same behavior against MP-12.

**Navigation controls**
- Each navigates to its destination in section 2. No writes.

## 7. Conditional behavior

| Trigger | Result |
|---|---|
| Organization has no item requests | Item selector empty state, per section 9 |
| Organization has no volunteer requests | Volunteer selector empty state, per section 9 |
| No selection made | `[CAPTURE]` whether the edit control is disabled |

## 8. Copy

| Context | Text |
|---|---|
| Page heading | `[CAPTURE]` |
| Item requests region heading | `[CAPTURE]` |
| Volunteer requests region heading | `[CAPTURE]` |
| Both selector labels or placeholders | `[CAPTURE]` |
| Both edit button labels | `[CAPTURE]` |
| Every navigation control label | `[CAPTURE]` |
| Any instructional copy | `[CAPTURE]` |

## 9. Empty states

A newly approved organization arrives here with nothing in either selector, so this is the first screen every new member org sees. It is not an edge case.

`[CAPTURE]` what each region renders with no requests: an empty selector, a message, a hidden region, or a disabled control. Capture both regions independently; they may behave differently.

## 10. Mobile differences

`[CAPTURE]`. Expect the two request regions to stack and the navigation controls to change presentation.

## 11. Authorization

Resolve the session's selected organization per MP-02, then query requests filtered by that `org_id`, server-side. The organization is never read from the URL, a query parameter, or a form field. There is no route shape on this surface that accepts an organization identifier, and none should be added.

## 12. Error paths

| Failure | Rendered result |
|---|---|
| Session has no resolvable organization | Handled by MP-02, not here |
| Request query fails | A stated error in place of the selector. Never an empty selector, which is indistinguishable from having no requests |

That distinction is worth building carefully. A failed query that renders as an empty list tells a member their requests are gone.

## 13. Out of scope for this surface

- Creating, editing, or archiving anything. This surface only routes.
- Any supporter or pledge data. That is MP-13.
- Aggregate counts, totals, or metrics of any kind. Reporting is phase two and an agent that adds a "requests fulfilled" tile here has violated `Handbook.md` section 17.

## 14. Acceptance

- Both selectors list only the session organization's requests.
- Option labels match the captured format exactly, including separator and date format.
- Selecting a request and activating edit reaches the correct edit surface for that request.
- All seven navigation destinations resolve.
- Empty states render correctly for an organization with no requests of either type.
- No organization identifier appears in the URL.
- A member of another organization cannot see this organization's requests by any means.
- Desktop and mobile match their screenshots.

## 15. Open captures

| What is needed | Source |
|---|---|
| Selector option label format: exact separator and date format | Monday capture walkthrough |
| Which statuses appear in each selector | Monday capture walkthrough |
| Sort order of each selector | Monday capture walkthrough |
| Whether edit controls are disabled without a selection | Monday capture walkthrough |
| All copy in section 8 | Monday capture walkthrough |
| Empty state behavior, both regions independently | Monday capture walkthrough |
| Region order and navigation presentation | Desktop screenshot |
| Mobile structure | Mobile screenshot |
