# MP-10 — Volunteer request, create

| | |
|---|---|
| **Route** | `/dashboard/volunteer/new` |
| **Access** | Authenticated member with an active membership |
| **Bound** | Yes, matches screenshots, with one deliberate addition |
| **Screenshots** | `docs/screenshots/MP-10-desktop.png`, `docs/screenshots/MP-10-mobile.png` |
| **Depends on** | MP-04 (entry), MP-11 (exit), storage adapter (D39) |
| **Spec status** | Complete |

---

## 1. Purpose

The first step of creating a volunteer request. A member describes the opportunity, where it happens, and who to contact. The request is created at `draft` and the member is sent to MP-11 to add the individual roles. Structurally parallel to MP-07.

## 2. Entry and exit

**Arrives from:** MP-04.
**Leaves to:** MP-11 `/dashboard/volunteer/:id/add`, automatic redirect on save. Back control returns to MP-04.

## 3. Data

**Reads:** `organizations.name` for the session organization.

**Writes, in one transaction:** `volunteer_requests` at `status = 'draft'`, `org_id` from the session, `created_by` set to the acting user. `people`, or resolves an existing row by `lower(email)`, then sets `contact_person_id`.

**Functions called:** None.

**Never touches:** `submitted_at`, `approved_at`, `approved_by`, `archived_at`, `archived_reason`, `expires_on`. No `approval_events` row; the first event is written at MP-11 on submit.

## 4. Layout regions

Confirmed order: hero text, two paragraphs of instructional copy, organization (read-only), contact fields (first name, last name, email, phone), deadline type and conditional date picker, details, request title, request description, volunteer role location, people helped, save button. No back control visible in this capture; include `< Back` for consistency with every other creation surface unless live site proves otherwise.

No image upload control on this surface, confirmed absent (D11, staff add images at approval).

## 5. Fields

| # | Label (verbatim) | Control | Required | Binds to | Validation |
|---|---|---|---|---|---|
| 1 | Request Title | Text | Yes | `volunteer_requests.title` | Non-empty |
| 2 | Request Description | Textarea | Yes | `volunteer_requests.description` | |
| 3 | Details | Text | Yes | `volunteer_requests.details` | Helper copy, verbatim: "Please include the following details:" then "Ongoing: Please share the frequency and time commitment for these roles," example "Ex: 1 hour/month, every second Tuesday, intermittently during the year, etc.," then "Date Specific: Please include the date or dates you need help," example "Ex: Saturday June 5th 6-9pm, the weekend of November 25-26, etc." |
| 4 | Volunteer Role Location | Text | Yes | `volunteer_requests.event_location` | Helper copy, verbatim: "Does this role take place a your location or at another site in the community?" (typo preserved, "take place a your location"), examples: "Our Warehouse \| Community Park \| Specific Address." |
| 5 | How many kids and/or families will this help? | Number | Yes | `volunteer_requests.people_helped` | Integer, zero or greater |
| 6 | Deadline Type | Radio group | Yes | `volunteer_requests.deadline_type` | Exactly two options, confirmed: Ongoing and Date Specific |
| 7 | **New field. See below** | Date picker | Conditionally | `volunteer_requests.deadline_date` | Required when field 6 is Date Specific |
| 8 | First Name | Text | Yes | `people.first_name` | Non-empty |
| 9 | Last Name | Text | Yes | `people.last_name` | Non-empty |
| 10 | Email | Email | Yes | `people.email` | Valid email |
| 11 | Phone Number | Text | Yes | `people.phone` | |
| 12 | Organization | Read-only text | n/a | `organizations.name`, session organization | Display only, never submitted |

**Contact name confirmed as two separate inputs already**, First Name and Last Name. No deviation needed here; this form already matches D41.

**`description` and `details` are two separate columns and two separate inputs.** Field 3's helper copy distinguishes them on screen.

**Fields 9 and 10 are two separate inputs and stay that way**, per `Handbook.md` section 8. They are stored as two columns. Never concatenate.

### Date picker (deviation one)

Volunteer requests gain a deadline date. The current surface carries a deadline type with no date field to pair with it, which leaves the type selector describing a date that does not exist.

The date picker appears when Deadline Type is Date Specific and behaves like MP-07's date picker component. Volunteer deadline options are two (Ongoing, Date Specific), not MP-07's three. This is the only addition to this surface and it is already approved; see `Handbook.md` section 4.2, deviation one.

## 6. Actions

**Continue to Adding Roles**
- Enabled when: all required fields pass validation.
- Does: the write in section 3.
- Transaction boundary: the request and the contact person together or not at all.
- Emails queued: none.
- On success: redirect to MP-11 for the new request.
- On failure: nothing written, form retains every value, failure message renders.

**Back control**
- Navigates to MP-04. No write.

## 7. Conditional behavior

| Trigger | Result |
|---|---|
| Deadline type is Date Specific | Date picker visible and required |
| Deadline type is Ongoing | Date picker hidden, `deadline_date` left null |

Volunteer requests use two deadline options only (Ongoing, Date Specific), not the item side's three. Same date-picker component as MP-07 when Date Specific is selected.

## 8. Copy

| Context | Text |
|---|---|
| Hero text | Volunteer Request |
| Page banner | POST VOLUNTEER ROLES |
| Instructional copy, paragraph 1 | Please complete the form below to create a new volunteer request. On this page you will provide the details about the child, teen, parent or caregiver this opportunity will serve them you will be able to add volunteer roles to the request on the following page. |
| Instructional copy, paragraph 2 | Make your request compelling by keeping the post title and description brief but including details about the impact this role will make for kids/families to inspire people to sign up. |
| Save button | Continue to Adding Roles |
| Failure message | Something went wrong and your request wasn't saved. Please try again. |
| Field-level validation messages | Standard per-field required messaging, nothing site-specific captured |
| Back control | < Back (included for consistency with other creation surfaces; not visible in this capture) |

Paragraph 1 is reproduced exactly as captured, including its run-on construction. Do not correct the grammar; verbatim is verbatim.

No image upload field exists on this surface (D11). Image-upload failure is not applicable.

## 9. Empty states

None.

## 10. Mobile differences

Closed per D60. Single column, same fields in the same order, full-width controls.

## 11. Authorization

Verify the session user holds an active membership, server-side. `org_id` comes from the session, never from field 12.

## 12. Error paths

| Failure | Rendered result |
|---|---|
| Contact email matches an existing person | Not an error. Attach the existing row |
| Single-word name in either name field | Succeeds. Stored as entered, no parsing |
| Transaction fails partway | Nothing written. Every value retained |
| Deadline type is `date_specific` with no date | Blocked at the surface |

## 13. Out of scope for this surface

- Adding roles. MP-11.
- Submitting for approval. MP-11.
- Any status other than `draft`.
- Setting `expires_on`.

## 14. Acceptance

- A saved request lands at `status = 'draft'` with `org_id` from the session.
- Saving redirects to MP-11 for that request.
- The deadline date field is present when Date Specific is selected, and behaves like MP-07's date picker component.
- First and last name land in two separate columns, never concatenated.
- A single-word name in either field succeeds.
- `description` and `details` land in their own columns and are not merged.
- No `approval_events` row and no email are produced here.
- The organization display field cannot influence which organization the request is created against.
- Field order, labels, and copy match the desktop screenshot, allowing for the added date field. Mobile matches the mobile screenshot.

## 15. Open captures

None.
