# Love in Action / Area Needs Database

A marketplace connecting [The Alliance](https://thealliancerc.org)'s member organizations (Roseville, CA) to the public. Organizations post material and volunteer needs; the public claims items or expresses interest in roles. Alliance staff approve organizations, members, and requests before anything is public.

This repository is the **build contract, source assets, and migration pipeline** for the August 17–21, 2026 rebuild — an [SVP AI for Good Hackathon](https://www.socialventurepartners.org) project. Application code (Node/TypeScript on Replit) is not in this tree yet; everything needed to build and migrate the live Wix system is here.

**Start here:** [`Handbook.md`](Handbook.md) is the authoritative build contract. [`replit.md`](replit.md) is the agent rules file — read it first on every task. [`SPRINT.md`](SPRINT.md) coordinates the week for people, not agents.

---

## What this project is

The Alliance coordinates a network of roughly 105 member organizations serving vulnerable children and families across the greater Sacramento region. Love in Action (LIA) is their **Area Needs Database** — the system those organizations use to publish needs and the public uses to respond.

| Actor | What they do |
|---|---|
| **Public visitor** | Browse active requests, claim items, express volunteer interest, subscribe to the weekly digest |
| **Org member** | Create and edit their organization's requests, invite members, remove members |
| **Staff approver** | Approve organizations, members, and requests |
| **Staff admin** | Everything above, plus people review, populations, email log, audit trail, subscribers |

**Stakes:** This system is live and in use by real organizations meeting real material needs. Data loss is unacceptable. Silent failure is worse than loud failure.

**Privacy:** The people served by these needs are never named in this system. There is no recipient table and no plan for one.

---

## Current status

| Area | Status |
|---|---|
| Build contract | Complete — `Handbook.md`, 26 surface specs, `DECISIONS.md` |
| Database schema | `migrations/0001_initial_schema.sql` — 17 tables, complete initial schema |
| Design tokens | `docs/Design.md` — colors, type, card tokens from the live site |
| Brand assets | `assets/` — Alliance logos, page headers, member-dashboard graphics. **Member-organization logos are not in the LIA database.** They exist on the public Alliance site at https://www.defendingthecause.org/non-profits (62 organizations). `organizations.logo_url` stays in the schema and is populated in phase two when the per-organization shareable page is built. That page is not being retired, so there is no urgency. |
| Migration transform | `transform.py` + `load.sql` — verified against the Aug 14 export. Produces 13 table CSVs; **does not produce `users`, `org_memberships`, or `digest_subscribers`** (contacts export required) |
| Legacy export audit | `docs/migration/data-audit.md` — measured from Aug 14 export |
| Surface screenshots | `docs/screenshots/` — folder exists; captures pending Monday walkthrough |
| Email body copy | Subjects known; twelve bodies not yet captured from sending platform |
| Contacts export | Not yet received — blocks login, memberships, digest subscribers |
| Application code | Not in this repo — builds on Replit, mirrored to GitHub |
| `schema.sql` | Not in repo yet — generated by `pg_dump` after migrations apply |

---

## Stack

| Layer | Choice |
|---|---|
| Runtime | Node, TypeScript strict, deployed on Replit |
| Database | Replit-managed Postgres (Helium). Portable by driver choice, not by migration plan (D58). |
| Auth | Better Auth (self-hosted in Postgres), magic link login (D40) |
| Email | Resend with SPF/DKIM/DMARC |
| Storage | Replit object storage for sprint (single adapter module, D39) |

All database access goes through one data-access layer. Migrations are numbered SQL in `/migrations`. Timestamps stored UTC, rendered America/Los_Angeles.

---

## Surfaces

Twenty-six surfaces across three areas. Each has a spec in `docs/specs/`. Bound surfaces (18) also require desktop and mobile screenshots before they are built.

**Member portal (MP-01–MP-13)** — login, signup, dashboard, requests, supporters. Thirteen bound surfaces; Christina's acceptance walkthrough covers the full member journey.

**Public browse (PB-01–PB-05)** — item and volunteer browse/detail, digest subscribe. Five bound surfaces; highest public traffic.

**Staff admin (ADMIN-01–ADMIN-08)** — approval queues, people review, populations, email log, audit, subscribers. Eight surfaces, not bound; specs are the design, no screenshots.

See `Handbook.md` section 11 for routes and `SPRINT.md` for the Monday capture walkthrough checklist.

---

## Migration pipeline

Legacy data lives in six Wix CMS collections. The transform is offline — no application code, no ORM — so a human can review CSVs before anything touches the database.

```bash
# Place six CMS exports in data/legacy-export/ (see data/README.md)
python3 transform.py --source data/legacy-export --out data/load

# Apply schema, then load (requires DATABASE_URL)
psql "$DATABASE_URL" -f migrations/0001_initial_schema.sql
psql "$DATABASE_URL" -f load.sql

# Validate
psql "$DATABASE_URL" -f docs/migration/validation.sql
```

**Rules:** `docs/migration/field-map.md` (contract). **Measurements:** `docs/migration/data-audit.md` (Aug 14 export).

The transform produces thirteen table CSVs plus `redirects.csv`, `migration-exceptions.csv`, and a transform report. It does **not** produce `users`, `org_memberships`, or `digest_subscribers` — those require the separate contacts export, which has not been received.

`data/legacy-export/` and `data/load/` are gitignored (PII). Do not commit them.

---

## Build lanes

From `Handbook.md` section 15. Lane 0 (migrations, data-access contracts, auth, seed) blocks everything Monday morning.

| Lane | Scope |
|---|---|
| **0** | Migrations, data-access signatures, route table, auth wiring, seed script |
| **A** | Member portal MP-01–MP-13 |
| **B** | Public PB-01–PB-05 + twelve email templates |
| **C** | Admin ADMIN-01–ADMIN-08 + migration and validation |

---

## Where to look

| File | Role |
|---|---|
| [`Handbook.md`](Handbook.md) | Authoritative build contract: model, invariants, surfaces, definition of done |
| [`replit.md`](replit.md) | Rules that are never broken; where to look for everything else |
| [`DECISIONS.md`](DECISIONS.md) | Numbered decisions (D1–D49) that specs and schema must honor |
| [`OPEN-ITEMS.md`](OPEN-ITEMS.md) | Every open question, organized by who can answer it |
| [`SPRINT.md`](SPRINT.md) | Week schedule, capture kit, testing, judging — for people, not agents |
| [`docs/Design.md`](docs/Design.md) | Color, type, and card tokens from the live site |
| [`docs/specs/{ID}.md`](docs/specs/) | One surface per file (MP, PB, ADMIN) |
| [`docs/screenshots/`](docs/screenshots/) | Desktop and mobile captures — **pending Monday walkthrough** |
| [`docs/email/TEMPLATES.md`](docs/email/TEMPLATES.md) | Twelve email templates (subjects known; bodies pending) |
| [`docs/migration/field-map.md`](docs/migration/field-map.md) | Source export → target schema, field by field |
| [`docs/migration/data-audit.md`](docs/migration/data-audit.md) | Measured export volumes and field-type findings |
| [`docs/migration/exclusions.csv`](docs/migration/exclusions.csv) | Records excluded from import, with a reason each |
| [`docs/migration/dropped-fields.csv`](docs/migration/dropped-fields.csv) | Every source field that does not reach the database, with a reason |
| [`docs/migration/validation.sql`](docs/migration/validation.sql) | Post-load validation queries |
| [`migrations/0001_initial_schema.sql`](migrations/0001_initial_schema.sql) | Complete initial schema (17 tables) |
| [`transform.py`](transform.py) | Offline Wix CMS → Postgres CSV transform |
| [`load.sql`](load.sql) | Generated `\copy` loader (gitignored; produced by transform) |
| [`assets/`](assets/) | Logos, page headers, member-dashboard graphics |

`schema.sql` (current schema as a single `pg_dump` file) is referenced by the handbook and is not in the repo yet. Generate after applying migrations: `pg_dump "$DATABASE_URL" --schema-only --no-owner --no-privileges > schema.sql`.

---

## Rules that are never broken

Eleven invariants from `replit.md`. Violating any is a defect.

1. No text-matching joins — people resolve to organizations through `org_memberships` only
2. `quantity_claimed` and `quantity_interested` written by database functions only
3. `quantity_remaining` is generated, never written
4. Every status transition writes an `approval_events` row in the same transaction
5. Every email send writes an `email_log` row before dispatch
6. `legacy_wix_id` is never a foreign key
7. Deletion is not a workflow — members removed by status, requests archived
8. Authorization is server-side on every request, before the query
9. No secrets in the repo
10. People served by needs are never named in this system
11. A person is identified by email, never by name — two inputs, two columns, stored as entered

---

## File tree

```
Alliance_LIADB_Build/
├── README.md
├── Handbook.md                          Build contract
├── replit.md                            Agent rules — read first every task
├── DECISIONS.md                         Settled decisions D1–D49
├── OPEN-ITEMS.md                        Open questions by owner
├── SPRINT.md                            Week schedule and coordination
├── transform.py                         Offline Wix → Postgres transform
├── load.sql                             Generated loader (gitignored — not present in a fresh clone until you run transform.py)
├── .gitignore
├── assets/
│   ├── alliance-logo-blue.png
│   ├── alliance-logo-gradient.png
│   ├── headers/
│   │   ├── LIA Email Header.png
│   │   ├── LIA Main Page Header-no words.jpg
│   │   ├── LIA Main Page Header.png
│   │   ├── Provide an Item Header.png
│   │   └── Volunteer your Time Header.png
│   └── member_dashboard_graphics/
│       ├── Member Dashboard AMC Login.png
│       ├── Member Dashboard Donors-Volunteers.png
│       ├── Member Dashboard Header.png
│       ├── Member Dashboard Item Request.png
│       ├── Member Dashboard Volunteer Request.png
│       ├── Member Dashboard-Add Users.png
│       └── My Org.png
├── data/                                gitignored — contains PII
│   ├── README.md                        Export placement and naming rules
│   ├── legacy-export/                   Six Wix CMS CSVs (local only)
│   └── load/                            Thirteen table CSVs + reports (local only)
├── docs/
│   ├── Design.md
│   ├── email/
│   │   └── TEMPLATES.md
│   ├── migration/
│   │   ├── data-audit.md                Measured export findings (Aug 14)
│   │   ├── exclusions.csv               Rows excluded from import
│   │   ├── field-map.md                 Source → target field mappings
│   │   └── validation.sql               Post-load validation queries
│   ├── screenshots/                     Pending Monday capture walkthrough
│   └── specs/
│       ├── _TEMPLATE.md
│       ├── ADMIN-01.md … ADMIN-08.md    Staff admin (8 surfaces)
│       ├── MP-01.md … MP-13.md          Member portal (13 surfaces)
│       └── PB-01.md … PB-05.md          Public browse (5 surfaces)
└── migrations/
    └── 0001_initial_schema.sql          Complete initial schema (17 tables)
```

Captured August 15, 2026.
